get:
  tags:
    - KDS
  summary: 
  description: 
  operationId: menuSubcategoryList
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/MenuSubcategoryList.yaml'
    required: true
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: '../schemas/MenuSubcategoryListResponse.yaml'
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '../schemas/Error.yaml'
    '500':
      description: Error
      content:
        application/json:
          schema:
            $ref: '../schemas/Error.yaml'
            example: {
              "code": 500,
              "message": "restaurant id does not exist"
            }
  security:
    - bearerAuth: []