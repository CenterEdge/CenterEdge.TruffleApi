post:
  tags:
    - KDS
  summary: 
  description: 
  operationId: createTaxes
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/createTaxes.yaml'
    required: true
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: '../schemas/BaseResponse.yaml'
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '../schemas/Error.yaml'
    '500':
      description: Error
      content:
        application/json:
          schema:
            $ref: '../schemas/Error.yaml'
            example: {
              "code": 500,
              "message": "restaurant id does not exist"
            }
  security:
    - bearerAuth: []